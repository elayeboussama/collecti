import { createTheme } from '@rneui/themed';

const theme = createTheme({
  lightColors: {
    primary: '#432C7A',
  },
  darkColors: {
    primary: '#000',
  },
  mode: 'light',
});


export default theme ;